# Closely AI - Environment Variables Specification

This document defines all configuration settings and secrets utilized by the Closely AI backend application, aligned with our core `config.py` Settings class.

---

## 1. System Variables Reference

| Variable Name | Description | Example / Default Value | Security Risk |
| :--- | :--- | :--- | :--- |
| **`APP_ENV`** | Defines operational runtime boundaries. | `development` (or `staging`, `production`) | Low |
| **`DATABASE_URL`** | PostgreSQL connection URI. | `postgresql://postgres:postgres@localhost:5432/closely_db` | Critical (Secrets) |
| **`REDIS_URL`** | Redis cache and Celery broker connection string. | `redis://localhost:6379/0` | High (Secrets) |
| **`GEMINI_API_KEY`** | Google Gemini API authentication key. | `AIzaSyD-12345...` | Critical (Secrets) |
| **`JWT_SECRET`** | Secret key used to sign and verify JWT authorization tokens. | `supersecretjwtkeyforcloselymvp2026!!!` | Critical (Secrets) |
| **`JWT_ALGORITHM`** | JWT encryption algorithm. | `HS256` | Medium |
| **`ACCESS_TOKEN_EXPIRE_MINUTES`**| Lifespan of JWT access tokens. | `1440` (24 hours) | Low |
| **`WHATSAPP_API_BASE_URL`** | Destination address for outbound Meta messages. | `https://graph.facebook.com` | Low |
| **`META_API_VERSION`** | Active target version of Meta Graph API. | `v19.0` | Low |
| **`WHATSAPP_PHONE_NUMBER_ID`**| Direct phone number identifier generated by Meta Developer dashboard. | `102837465910293` | High (Secrets) |
| **`WHATSAPP_ACCESS_TOKEN`** | Long-lived System User access token generated on Meta Console. | `EAAG...` | Critical (Secrets) |
| **`WHATSAPP_APP_SECRET`** | Meta App Secret token used to verify X-Hub signature headers. | `4a2e...` | Critical (Secrets) |
| **`WHATSAPP_VERIFY_TOKEN`** | Handshake validation string matching Meta Webhook App settings. | `closely_verify_token` | Medium |
| **`AWS_ACCESS_KEY_ID`** | AWS access key for media file S3 uploads. | `AKIA...` | High (Secrets) |
| **`AWS_SECRET_ACCESS_KEY`** | AWS secret key for media storage. | `wJalrXUtnFEMI/K7MDENG/bPxRfiCY...` | Critical (Secrets) |
| **`AWS_STORAGE_BUCKET_NAME`** | Target S3 bucket name. | `closely-media-storage` | Medium |
| **`AWS_S3_ENDPOINT_URL`** | Custom S3 API endpoint override (e.g. MinIO or Cloudflare R2). | `https://s3.amazonaws.com` | Low |
| **`SENTRY_DSN`** | Sentry integration URL for error alerting. | `https://key@sentry.io/project` | High |

---

## 2. Security Compliance Rules
* **No Repository Commits**: The `.env` file containing real values must *never* be committed to Git. Ensure `.env` is present in your project's `.gitignore` file.
* **Production Secrets**: In production, secrets must be loaded using an environment vault (e.g. AWS Secrets Manager, Doppler, or GitHub Actions secrets) rather than plaintext configuration files on servers.
* **API Rotations**: Generate fresh `JWT_SECRET` and `WHATSAPP_APP_SECRET` keys prior to every production release.
